{
  "name": "march-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/spec-schema && npm run build --workspace=apps/web && npm run build --workspace=apps/extension",
    "typecheck": "npm run typecheck --workspace=packages/spec-schema && npm run typecheck --workspace=apps/web && npm run typecheck --workspace=apps/extension",
    "package": "npm run build && npm run package --workspace=apps/extension"
  }
}
